### Initial Laravel installation(1st time only)
Install Laravel using `composer` utility service container.

`docker compose run --rm composer create-project --prefer-dist laravel/laravel /var/www/html`

Here `composer` is the container defined in `docker-compose.yaml` file